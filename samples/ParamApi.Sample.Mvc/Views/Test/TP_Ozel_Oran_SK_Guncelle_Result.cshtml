 @model TP_Ozel_Oran_SK_Guncelle_ResultModel
@{
    ViewData["Title"] = "TP_Ozel_Oran_SK_Guncelle Sonuçları";
}

<div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <div class="col-md-3">
            <partial name="_TestSidebar" />
        </div>

        <!-- Main Content -->
        <div class="col-md-9">
            <!-- Başlık ve Durum -->
            <div class="card mb-4">
                <div class="card-header @(Model.IsSuccess ? "bg-success" : "bg-danger") text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">
                            @if (Model.IsSuccess)
                            {
                                <i class="fas fa-check-circle"></i> @Html.Raw("TP_Ozel_Oran_SK_Guncelle İşlemi Başarılı")
                            }
                            else
                            {
                                <i class="fas fa-times-circle"></i> @Html.Raw("TP_Ozel_Oran_SK_Guncelle İşlemi Başarısız")
                            }
                        </h5>
                        <div class="text-end">
                            <small>@Model.ProcessTime.ToString("dd.MM.yyyy HH:mm:ss")</small><br>
                            <small>@Model.ProcessDuration ms</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Gönderilen Request Bilgileri -->
            @if (Model.Request != null)
            {
                <div class="card mb-4">
                    <div class="card-header bg-info text-white">
                        <h6 class="mb-0"><i class="fas fa-paper-plane"></i> Gönderilen Request Bilgileri</h6>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-3">
                                <h6>🆔 SK ID Bilgisi</h6>
                                <table class="table table-sm">
                                    <tr>
                                        <td><strong>SK ID:</strong></td>
                                        <td><span class="badge bg-primary">@Model.Request.Ozel_Oran_SK_ID</span></td>
                                    </tr>
                                </table>
                            </div>
                            <div class="col-md-9">
                                <h6>💰 Güncellenen Oranlar (%)</h6>
                                <div class="row">
                                    <div class="col-md-6">
                                        <table class="table table-sm">
                                            <tr><td>Tek Çekim:</td><td><strong>@Model.Request.MO_1%</strong></td></tr>
                                            <tr><td>2 Taksit:</td><td><strong>@Model.Request.MO_2%</strong></td></tr>
                                            <tr><td>3 Taksit:</td><td><strong>@Model.Request.MO_3%</strong></td></tr>
                                            <tr><td>4 Taksit:</td><td><strong>@Model.Request.MO_4%</strong></td></tr>
                                            <tr><td>5 Taksit:</td><td><strong>@Model.Request.MO_5%</strong></td></tr>
                                            <tr><td>6 Taksit:</td><td><strong>@Model.Request.MO_6%</strong></td></tr>
                                        </table>
                                    </div>
                                    <div class="col-md-6">
                                        <table class="table table-sm">
                                            <tr><td>7 Taksit:</td><td><strong>@Model.Request.MO_7%</strong></td></tr>
                                            <tr><td>8 Taksit:</td><td><strong>@Model.Request.MO_8%</strong></td></tr>
                                            <tr><td>9 Taksit:</td><td><strong>@Model.Request.MO_9%</strong></td></tr>
                                            <tr><td>10 Taksit:</td><td><strong>@Model.Request.MO_10%</strong></td></tr>
                                            <tr><td>11 Taksit:</td><td><strong>@Model.Request.MO_11%</strong></td></tr>
                                            <tr><td>12 Taksit:</td><td><strong>@Model.Request.MO_12%</strong></td></tr>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }

            <!-- Test Sonuçları -->
            <div class="card mb-4">
                <div class="card-header bg-warning text-dark">
                    <h6 class="mb-0"><i class="fas fa-edit"></i> TP_Ozel_Oran_SK_Guncelle Sonuçları</h6>
                </div>
                <div class="card-body">
                    @if (Model.Response != null)
                    {
                        <div class="row">
                            <div class="col-md-6">
                                <h6>🎯 Güncelleme Sonucu</h6>
                                <table class="table table-sm">
                                    <tr>
                                        <td><strong>Sonuç:</strong></td>
                                        <td><span class="badge @(Model.IsSuccess ? "bg-success" : "bg-danger")">@Model.Response.Sonuc</span></td>
                                    </tr>
                                    <tr>
                                        <td><strong>Sonuç Mesajı:</strong></td>
                                        <td>@Model.Response.Sonuc_Str</td>
                                    </tr>
                                    @if (Model.Request != null)
                                    {
                                        <tr>
                                            <td><strong>Güncellenen SK ID:</strong></td>
                                            <td><span class="badge bg-primary">@Model.Request.Ozel_Oran_SK_ID</span></td>
                                        </tr>
                                    }
                                </table>

                                @if (Model.IsSuccess)
                                {
                                    <div class="alert alert-success">
                                        <i class="fas fa-check-circle"></i>
                                        <strong>Güncelleme Başarılı!</strong><br>
                                        Özel oran değerleri başarıyla güncellendi.
                                    </div>
                                }
                                else
                                {
                                    <div class="alert alert-danger">
                                        <i class="fas fa-times-circle"></i>
                                        <strong>Güncelleme Başarısız!</strong><br>
                                        @Model.ErrorMessage
                                    </div>
                                }
                            </div>
                            <div class="col-md-6">
                                <h6>📊 İşlem Özeti</h6>
                                <div class="card border-light">
                                    <div class="card-body">
                                        @if (Model.Request != null)
                                        {
                                            <p class="mb-2">
                                                <i class="fas fa-hashtag text-primary"></i>
                                                <strong>SK ID:</strong> @Model.Request.Ozel_Oran_SK_ID
                                            </p>
                                            <p class="mb-2">
                                                <i class="fas fa-percentage text-success"></i>
                                                <strong>Tek Çekim:</strong> @Model.Request.MO_1%
                                            </p>
                                            <p class="mb-2">
                                                <i class="fas fa-calculator text-info"></i>
                                                <strong>En Yüksek Taksit:</strong> @Model.Request.MO_12% (12T)
                                            </p>
                                            <p class="mb-0">
                                                <i class="fas fa-clock text-warning"></i>
                                                <strong>İşlem Süresi:</strong> @Model.ProcessDuration ms
                                            </p>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            <i class="fas fa-exclamation-triangle"></i> Response bilgisi alınamadı.
                        </div>
                    }
                </div>
            </div>

            <!-- Raw JSON Response -->
            <div class="card mb-4">
                <div class="card-header bg-secondary text-white">
                    <h6 class="mb-0"><i class="fas fa-code"></i> Ham JSON Response</h6>
                </div>
                <div class="card-body">
                    <pre class="bg-dark text-light p-3 rounded"><code>@Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.Response, new System.Text.Json.JsonSerializerOptions { WriteIndented = true }))</code></pre>
                </div>
            </div>

            <!-- İşlem Geçmişi ve Linkler -->
            <div class="row">
                <div class="col-md-6">
                    <div class="card border-success">
                        <div class="card-header bg-success text-white">
                            <h6 class="mb-0"><i class="fas fa-clock"></i> İşlem Bilgileri</h6>
                        </div>
                        <div class="card-body">
                            <p class="card-text small mb-1">
                                <strong>İşlem Zamanı:</strong><br>
                                @Model.ProcessTime.ToString("dd.MM.yyyy HH:mm:ss")
                            </p>
                            <p class="card-text small mb-0">
                                <strong>İşlem Süresi:</strong> @Model.ProcessDuration ms
                            </p>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card border-warning">
                        <div class="card-header bg-warning text-dark">
                            <h6 class="mb-0"><i class="fas fa-link"></i> İlgili İşlemler</h6>
                        </div>
                        <div class="card-body">
                            <div class="d-flex gap-2 flex-wrap">
                                <a href="@Url.Action("TP_Ozel_Oran_SK_Guncelle", "Test")" class="btn btn-warning btn-sm">
                                    <i class="fas fa-edit"></i> Tekrar Güncelle
                                </a>
                                <a href="@Url.Action("TP_Ozel_Oran_SK_Liste", "Test")" class="btn btn-info btn-sm">
                                    <i class="fas fa-users"></i> SK Liste Kontrol Et
                                </a>
                                <a href="@Url.Action("TP_Ozel_Oran_Liste", "Test")" class="btn btn-primary btn-sm">
                                    <i class="fas fa-list"></i> Özel Oran Liste
                                </a>
                                <a href="@Url.Action("Index", "Test")" class="btn btn-secondary btn-sm">
                                    <i class="fas fa-home"></i> Ana Sayfa
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Sonraki Adımlar -->
            @if (Model.IsSuccess)
            {
                <div class="alert alert-info mt-4">
                    <h6><i class="fas fa-lightbulb"></i> Sonraki Adımlar</h6>
                    <p class="mb-2">Güncelleme işlemi başarılı! Şimdi yapabilecekleriniz:</p>
                    <ul class="mb-0">
                        <li><strong>Kontrol Et:</strong> <a href="@Url.Action("TP_Ozel_Oran_SK_Liste", "Test")" class="text-decoration-none">SK Liste</a> ile güncellenmiş değerleri kontrol edin</li>
                        <li><strong>Test Et:</strong> Güncellenen oranlarla yeni bir ödeme testi yapın</li>
                        <li><strong>Diğer SK'ları:</strong> Başka SK ID'leri için de güncelleme yapabilirsiniz</li>
                    </ul>
                </div>
            }
        </div>
    </div>
</div> 